<%= form_with(model: usuario_personagem) do |form| %>
  <% if usuario_personagem.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(usuario_personagem.errors.count, "error") %> prohibited this usuario_personagem from being saved:</h2>

      <ul>
        <% usuario_personagem.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.label :personagem_id, style: "display: block" %>
    <%= form.text_field :personagem_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
